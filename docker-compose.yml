services:
  mysqldb:
    image: mysql:8.0
    container_name: store_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: alphaspring_store
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: